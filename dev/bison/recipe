pkgname=bison
pkgver=3.8.1
arcext=tar.lz
arcurlbase=https://ftp.gnu.org/gnu/bison/
run_deps="readline"

# --enable-relocatable は指定しない（doc/relocatable.texi）。
# このプログラムだけリロケータブルになってもしかたがないと思う。
# なお指定するとサイズがちょっとだけ大きくなる（誤差だが）。

_config () {
    ./configure \
        --prefix="$prefix" \
        --disable-silent-rules \
        --disable-nls \
        CFLAGS="$optflags"
}
